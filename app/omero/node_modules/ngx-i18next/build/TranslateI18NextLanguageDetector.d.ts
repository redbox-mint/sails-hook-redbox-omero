export declare class TranslateI18NextLanguagesSupport {
    private locale;
    private static logger;
    constructor(locale: string);
    getSupportedLanguage(supportedLanguages?: Array<string>): string;
    private isLangSupported(lng, supportedLanguages?);
}
